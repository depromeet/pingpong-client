import type { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <div>
      hello world!
      <div className=""></div>
      <span>{data?.id}</span>
      <span>{data?.name}</span>
    </div>
  );
};

export default Home;
